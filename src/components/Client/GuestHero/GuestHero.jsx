import React from 'react'

const GuestHero = () => {
    return (
        <div>
            <h1>This is Guest Hero</h1>

            {/* <button onClick={handleLogout}>Log Out</button> */}
        </div>
    )
}

export default GuestHero
